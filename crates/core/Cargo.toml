[package]
name = "starbound-core"
version = "0.1.0"
edition = "2021"
description = "Core data types — the shared vocabulary of Starbound"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
strum = { workspace = true }
