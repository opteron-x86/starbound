[package]
name = "starbound-llm"
version = "0.1.0"
edition = "2021"
description = "LLM integration — prompt assembly, API calls, validation"

[dependencies]
starbound-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
